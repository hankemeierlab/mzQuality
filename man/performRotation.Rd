% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BatchEffects.R
\name{performRotation}
\alias{performRotation}
\title{Perform batch-wise rotation of assay values}
\usage{
performRotation(exp, orders, assay, qcColumns, batch, withinBatchNonNA = 3)
}
\arguments{
\item{exp}{A SummarizedExperiment object containing the experimental data.}

\item{orders}{A numeric vector specifying the injection order of the samples.}

\item{assay}{A numeric matrix or data frame containing the assay values to
be adjusted.}

\item{qcColumns}{A logical vector indicating which columns correspond to QC
samples.}

\item{batch}{A character or numeric value specifying the batch to process.}

\item{withinBatchNonNA}{An integer specifying the minimum number of non-NA
values required within a batch for reliable rotation. Defaults to 3.}
}
\value{
A numeric matrix with the rotated assay values, adjusted for
    systematic batch effects.
}
\description{
Adjusts assay values within a batch using a linear model
    constructed from quality control (QC) samples.
}
\details{
This function performs a batch-wise rotation of assay values to
    account for systematic variations within a batch. It uses QC samples to
    construct a linear model (slope and intercept) and applies this model to
    adjust the assay values for all samples in the batch. Values with too
    many missing data points are excluded from rotation.
}
\examples{
# Example usage:
# exp <- SummarizedExperiment(...)
# orders <- c(1, 2, 3, 4, 5)
# assay <- matrix(rnorm(50), nrow = 10)
# qcColumns <- c(TRUE, TRUE, FALSE, FALSE, TRUE)
# batch <- "Batch1"
# rotatedAssay <- performRotation(exp, orders, assay, qcColumns, batch)
}
